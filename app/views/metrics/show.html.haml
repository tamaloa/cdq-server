%p#notice= notice
= render partial: 'partials/nav_hierarchy', locals: {app: @metric.dimension.app, current_score: @metric.current_value,
                                                    dimension: @metric.dimension, metric: @metric}
%p
  %b Weight:
  = @metric.weight
  %p= @metric.description

= render partial: 'partials/timeline', locals: {thing: @metric}