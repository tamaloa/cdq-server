%h3 Development over Time

#chart_values{data: { values: thing.values_for_chart}}

#chart

#overview

%h3 Most recent values
%ul
- thing.last_values.each do |value|
  %li
    = link_to value.metric.name, value.metric if value.metric #TODO Fix values without metric??
    = value.stamp
    = value.value
    = link_to "Improvement Advice", value if value.improvement_advice.present?